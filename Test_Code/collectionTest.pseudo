NUMS = [2, 4, 6, 8, 11, 13, 15, 16, 1]
NUMSCOL = new Collection() 

loop I from 0 to NUMS.length() -  1
    if (NUMS[I] > 10) then
        NUMSCOL.add(NUMS[I])
        output NUMS[I]
    end if
end loop

FRUITCOL= new Collection() 
FRUITARR =["Apple","Cherry","Peach","Banana","Fig","Grape","Olive","Mango", "Pinapple"]

loop I from 0 to FRUITARR.length()-1
    FRUITCOL.add(FRUITARR[I])
end loop 
FRUITCOL.resetNext()
loop while FRUITCOL.hasNext()
    FRUIT = FRUITCOL.getNext()
        output FRUIT  
end loop
